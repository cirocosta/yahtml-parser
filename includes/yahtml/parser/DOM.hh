#ifndef YAHTML__DOM_HH
#define YAHTML__DOM_HH

#include <string>
#include <iostream>
#include <utility>
#include <map>

class HTMLDriver;
class DOM;

class DOM
{
public:
  DOM ();
  virtual ~DOM ();
};

#endif
